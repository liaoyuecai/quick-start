spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config
      profile: dev
      label: master

eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka/

endpoints:
  refresh:
    enabled: true
    sensitive: false
